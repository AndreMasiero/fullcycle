Projeto em TypeScript usando **Jest** para testes e **@swc/jest** para transpilar rapidamente.

## Requisitos
- Node.js >= 20 (recomendado LTS)
- npm >= 9

Verifique:
```bash
node -v
npm -v


npm install

npm test

npx jest --coverage
